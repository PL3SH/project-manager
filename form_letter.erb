<html>
<head>
  <meta charset='UTF-8'>
  <title>Thank You!</title>
</head>
<body>
  <!-- Muestra un saludo personalizado con el nombre del destinatario -->
  <h1>Thanks <%= name %></h1>
  <p>Thanks for coming to our conference.  We couldn't have done it without you!</p>

  <p>
    Political activism is at the heart of any democracy and your voice needs to be heard.
    Please consider reaching out to your following representatives:
  </p>
  <p> phone number:<%= phone %></p>
  <table>
    <%# Verifica si 'legislators' es un Array %>
    <% if legislators.kind_of?(Array) %>
      <th>Name</th><th>Website</th>
        <%# Itera sobre cada legislador en el array %>
        <% legislators.each do |legislator| %>
        <tr>
          <%# Muestra el nombre del legislador %>
          <td><%= "#{legislator.name}" %></td>
          <%# Muestra las URLs del legislador, si existen, unidas por espacios %>
          <td><%= "#{legislator.urls.join(" ")}" unless legislator.urls.nil? %></td>
        </tr>
        <% end %>
    <% else %>
      <%# Si 'legislators' no es un Array, muestra su contenido en una sola celda %>
      <th></th>
      <td><%= "#{legislators}" %></td>
    <% end %>
  </table>
</body>
</html>
